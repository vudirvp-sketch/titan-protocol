warning: in the working copy of 'PROTOCOL.base.md', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'scripts/enhanced_llm_query.py', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'scripts/test_navigation.py', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'skills/validators/no-todos.js', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'skills/validators/security.js', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/PROTOCOL.base.md b/PROTOCOL.base.md[m
[1mindex 82f4424..9d92399 100644[m
[1m--- a/PROTOCOL.base.md[m
[1m+++ b/PROTOCOL.base.md[m
[36m@@ -931,4 +931,3 @@[m [mThis protocol synthesizes:[m
 - Unified Severity Scale (SEV-1..4 across all registries)[m
 [m
 Architecture: deterministic, verifiable, rollback-safe, session-resumable, production-grade.[m
[31m-```[m
[1mdiff --git a/scripts/enhanced_llm_query.py b/scripts/enhanced_llm_query.py[m
[1mindex bcd3813..d08133f 100644[m
[1m--- a/scripts/enhanced_llm_query.py[m
[1m+++ b/scripts/enhanced_llm_query.py[m
[36m@@ -10,7 +10,7 @@[m [mimport time[m
 from dataclasses import dataclass, field[m
 from enum import Enum[m
 from typing import Optional, List, Dict, Any, Callable[m
[31m-from datetime import datetime[m
[32m+[m[32mfrom datetime import datetime, timezone[m
 [m
 [m
 class Confidence(Enum):[m
[36m@@ -37,7 +37,7 @@[m [mclass QueryResult:[m
     attempt: int[m
     fallback_used: bool[m
     fallback_reason: Optional[FallbackReason] = None[m
[31m-    timestamp: str = field(default_factory=lambda: datetime.utcnow().isoformat())[m
[32m+[m[32m    timestamp: str = field(default_factory=lambda: datetime.now(timezone.utc).isoformat())[m
 [m
 [m
 @dataclass[m
[36m@@ -50,10 +50,13 @@[m [mclass FallbackConfig:[m
     timeout_seconds: float = 30.0[m
     enable_model_fallback: bool = True[m
     alternative_models: List[str] = field(default_factory=lambda: [[m
[31m-        "primary",[m
[31m-        "alternative-1",[m
[31m-        "alternative-2"[m
[32m+[m[32m        "primary",  # TODO: Replace with actual model identifier[m
[32m+[m[32m        "alternative-1",  # TODO: Replace with actual model identifier[m
[32m+[m[32m        "alternative-2"  # TODO: Replace with actual model identifier[m
     ])[m
[32m+[m[32m    # WARNING: Default model names are placeholders.[m
[32m+[m[32m    # Set alternative_models to actual model identifiers before production use,[m
[32m+[m[32m    # or load from config.yaml[m
 [m
 [m
 class EnhancedLLMQuery:[m
[36m@@ -244,7 +247,7 @@[m [mclass EnhancedLLMQuery:[m
             "chunk_id": chunk_id,[m
             "task": task[:200],  # Truncate for storage[m
             "reason": last_reason.value if last_reason else "UNKNOWN",[m
[31m-            "timestamp": datetime.utcnow().isoformat(),[m
[32m+[m[32m            "timestamp": datetime.now(timezone.utc).isoformat(),[m
             "attempts": attempt[m
         }[m
         self._gap_log.append(gap_entry)[m
[1mdiff --git a/scripts/test_navigation.py b/scripts/test_navigation.py[m
[1mindex e69cdf2..426df1f 100644[m
[1m--- a/scripts/test_navigation.py[m
[1m+++ b/scripts/test_navigation.py[m
[36m@@ -365,7 +365,7 @@[m [mdef main():[m
 [m
     # Output JSON report[m
     report = {[m
[31m-        "timestamp": __import__("datetime").datetime.utcnow().isoformat() + "Z",[m
[32m+[m[32m        "timestamp": __import__("datetime").datetime.now(__import__("datetime").timezone.utc).isoformat().replace("+00:00", "Z"),[m
         "repo_root": str(repo_root),[m
         "summary": {[m
             "passed": tester.passed,[m
[1mdiff --git a/skills/validators/no-todos.js b/skills/validators/no-todos.js[m
[1mindex 868fccb..d05660c 100644[m
[1m--- a/skills/validators/no-todos.js[m
[1m+++ b/skills/validators/no-todos.js[m
[36m@@ -32,11 +32,17 @@[m [mmodule.exports = {[m
     const violations = [];[m
 [m
     for (const pattern of this.patterns) {[m
[31m-      const matches = content.match(new RegExp(pattern, 'g'));[m
[32m+[m[32m      // FIX: Use pattern.source to properly extract regex source[m
[32m+[m[32m      // and preserve flags from original pattern[m
[32m+[m[32m      const flags = pattern.flags || 'g';[m
[32m+[m[32m      const globalPattern = new RegExp(pattern.source, flags);[m
[32m+[m[32m      const matches = content.match(globalPattern);[m
       if (matches) {[m
         // Find line numbers[m
         const lines = content.split('\n');[m
         lines.forEach((line, index) => {[m
[32m+[m[32m          // FIX: Reset lastIndex before test() to avoid state issues[m
[32m+[m[32m          pattern.lastIndex = 0;[m
           if (pattern.test(line)) {[m
             violations.push({[m
               line: index + 1,[m
[1mdiff --git a/skills/validators/security.js b/skills/validators/security.js[m
[1mindex df8c95d..550cf98 100644[m
[1m--- a/skills/validators/security.js[m
[1m+++ b/skills/validators/security.js[m
[36m@@ -68,10 +68,12 @@[m [mmodule.exports = {[m
 [m
     // Check critical patterns[m
     for (const pattern of this.criticalPatterns) {[m
[32m+[m[32m      pattern.lastIndex = 0; // FIX: Reset lastIndex for global regex[m
       const matches = content.match(pattern);[m
       if (matches) {[m
         const lines = content.split('\n');[m
         lines.forEach((line, index) => {[m
[32m+[m[32m          pattern.lastIndex = 0; // FIX: Reset before each test() call[m
           if (pattern.test(line)) {[m
             violations.push({[m
               line: index + 1,[m
[36m@@ -88,10 +90,12 @@[m [mmodule.exports = {[m
 [m
     // Check warning patterns[m
     for (const pattern of this.warningPatterns) {[m
[32m+[m[32m      pattern.lastIndex = 0; // FIX: Reset lastIndex for global regex[m
       const matches = content.match(pattern);[m
       if (matches) {[m
         const lines = content.split('\n');[m
         lines.forEach((line, index) => {[m
[32m+[m[32m          pattern.lastIndex = 0; // FIX: Reset before each test() call[m
           if (pattern.test(line)) {[m
             violations.push({[m
               line: index + 1,[m
[36m@@ -108,10 +112,12 @@[m [mmodule.exports = {[m
 [m
     // Check info patterns[m
     for (const pattern of this.infoPatterns) {[m
[32m+[m[32m      pattern.lastIndex = 0; // FIX: Reset lastIndex for global regex[m
       const matches = content.match(pattern);[m
       if (matches) {[m
         const lines = content.split('\n');[m
         lines.forEach((line, index) => {[m
[32m+[m[32m          pattern.lastIndex = 0; // FIX: Reset before each test() call[m
           if (pattern.test(line)) {[m
             violations.push({[m
               line: index + 1,[m
